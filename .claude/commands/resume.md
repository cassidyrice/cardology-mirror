---
description: Pick up where the last session left off, in three lines
---
Read `~/cardblueprints-ops/STATE.md`, then run `git -C ~/cardology-elroy-qa status --short | head` and `git -C ~/cardology-elroy-qa log --oneline -3`.
Reply in at most three lines: what is live, what is uncommitted, the single next action. End with "(y/n)".
